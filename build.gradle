buildscript {
    apply from: 'dependency_version.properties'

    repositories {
        mavenCentral()
        jcenter() // for gradle-versions-plugin
    }
}

plugins {
    id 'com.avast.gradle.docker-compose' version "$avastDockerComposeGradleVersion"
}

