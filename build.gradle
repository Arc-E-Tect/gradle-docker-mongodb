buildscript {
    apply from: 'dependency_version.properties'

    repositories {
        mavenCentral()
        jcenter() // for gradle-versions-plugin
    }
}

plugins {
    id "com.palantir.docker-compose" version "${palantirDockerGradleVersion}"
}

